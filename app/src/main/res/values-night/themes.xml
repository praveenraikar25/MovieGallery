<resources>
    <!-- Launch theme only; all real theming is done via Compose MaterialTheme in ui/theme. -->
    <style name="Theme.MovieGallery" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/app_bg_dark</item>
        <item name="android:statusBarColor">@color/app_bg_dark</item>
        <item name="android:navigationBarColor">@color/app_bg_dark</item>
    </style>
</resources>
