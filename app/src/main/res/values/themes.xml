<resources>
    <!-- Launch theme only; all real theming is done via Compose MaterialTheme in ui/theme. -->
    <style name="Theme.MovieGallery" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_bg_light</item>
        <item name="android:statusBarColor">@color/app_bg_light</item>
        <item name="android:navigationBarColor">@color/app_bg_light</item>
    </style>
</resources>
